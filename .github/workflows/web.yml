name: web
on:
  push:
    paths:
      - "./web"
      - ".github/workflows/web.yml"
  workflow_dispatch:

jobs:
  build:
    runs-on: golang:1.21-alpine
    steps:
      - run: go build
        working-directory: ./web
      - run: go test
        working-directory: ./web